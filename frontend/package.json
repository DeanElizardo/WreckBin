{
  "name": "react-wreckbin",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.27.2",
    "bootstrap": "^5.2.1",
    "install": "^0.13.0",
    "npm": "^8.19.1",
    "react": "^18.2.0",
    "react-code-blocks": "^0.0.9-0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.4.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "doppler run react-scripts start",
    "build": "react-scripts build && rm -r ../backend/build && cp -r build ../backend",
    "test": "doppler run react-scripts test",
    "eject": "doppler run react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "json-server": "^0.17.0"
  },
  "proxy": "http://localhost:3030"
}
